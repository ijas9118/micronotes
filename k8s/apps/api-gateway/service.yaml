apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: app-services
spec:
  selector:
    app: api-gateway
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
